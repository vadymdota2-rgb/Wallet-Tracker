# Карта бандла → исходники

Восстановление ведётся из `html/assets/routes.js` (400 КБ, сборка Vite).
Исходников проекта не существует: приложение делали в конструкторе Grok,
в репозиторий положили только результат сборки. Эта таблица — то, что
удалось установить по коду, чтобы не разбирать бандл каждый раз заново.

## Что в бандле НЕ является кодом приложения

| имя | размер | что это |
|---|---|---|
| `o`, `D`, `Ne`, `ve` и соседние | ~66 КБ | библиотека тостов **sonner** |
| `w` | 1.4 КБ | хранилище **zustand** (persist) |
| `i18nPack` + словари | ~198 КБ | 16 языков × 425 ключей |

Всё это при пересборке возвращается зависимостями из `package.json`
либо (словари) уже вынесено в `src/i18n/`.

**Собственного кода приложения — около 75 КБ.**

## Экраны

| имя | размер | экран | опознан по |
|---|---|---|---|
| `yt` | 2.0 КБ | оболочка: шапка, меню, вкладки | `.app`, `.brand`, `.hbtn` |
| `Je` | 3.3 КБ | **Мои кошельки** (главный) | `myWallets`, `alertThreshold` |
| `at` | 3.2 КБ | карточка кошелька | `accountValue`, `confirmRemove` |
| `Ye` | 4.8 КБ | Рынок: поток, жара, ротация | `byAccel`, `byCoins`, `aprOi` |
| `rt` | 2.0 КБ | монета: график, консенсус | `candlesNote`, `consLead` |
| `et` | 2.3 КБ | Cortex: сигналы | `coreCap`, `lockedCortex` |
| `it` | 1.6 КБ | карточка сигнала | `entryLev`, `confidence` |
| `st` | 1.7 КБ | позиция: ликвидация, фандинг | `liqCushion`, `isolated` |
| `tt` | 2.5 КБ | трейдер из топа | `avgHold`, `days90` |
| `ct` | 1.5 КБ | строка трейдера | `avgTrade`, `daysActive` |
| `ht` | 1.3 КБ | Премиум | `premAlerts`, `premDays` |
| `_t` | 1.0 КБ | Помощь | `helpCortexB`, `helpNumsB` |
| `lt` | 1.0 КБ | добавление кошелька | `addrLabel`, `freeSlots` |
| `dt` | 0.8 КБ | отчёт по чужому адресу | `addThis`, `profitFactor` |
| `Qe` | 1.4 КБ | блок алертов и порога | `alertLead`, `alertsCap` |
| `$e` | 1.2 КБ | заглушка «нужен Премиум» | `lockedPos`, `lockedCta` |

## Общие компоненты

| имя | что делает |
|---|---|
| `q` | иконка монеты: локальные файлы → Hyperliquid → PancakeSwap → CoinGecko |
| `Y` | строка списка: иконка, заголовок, значение, подпись |
| `Z` | кнопка-действие во всю ширину |
| `J` | заголовок раздела с примечанием справа |
| `X` | сетка плиток «подпись / значение» |
| `Ie` | «пульс» сверху: сумма за сутки и полоса купли-продажи |
| `Ve` | пустой экран с подсказкой |
| `Tv` | график свечей |
| `Be` | отрисовка графика (`.chart`) |
| `Fe` | полосы сравнения (`.bar`, `.cl`, `.cm`) |
| `K` | сетка `.d1`–`.d4` |
| `S` | фоновая анимация (`.bg-canvas`) |
| `_` | шаг анимации фона |
| `v` | визуал «16 признаков» (`.core-cv`) |
| `g` | заставка при запуске |
| `f` | выдвижное меню |

## Вспомогательное

| имя | что делает |
|---|---|
| `L` | форматирование: `usd`, `pct`, `px`, `g` — разряды запятой, стиль Binance |
| `W` | доступ к store (zustand) |
| `p` | подписка на живые данные |
| `$` | функция перевода `t(key, params)` |
| `H(план, основной)` | кошелёк на паузе: бесплатный план и не основной |
| `V(план)` | лимит кошельков: 50 у премиума, иначе 1 |
| `U` | тактильный отклик Telegram |
| `next` | обход списка адресов графиков с запасными вариантами |

## Данные приложения

Store (`W`) держит: `lang`, `tab`, `stack`, `menuOpen`, `market`, `win`,
`venue`, `wallets`, `plan`, `threshold`, `premUntil`, `syncedAt`,
`setThreshold`, `setPrimary`, `goTab`, `open`, `setPeople`.
Сохраняется в localStorage под ключом `wt-miniapp-v6`.

Поля кошелька: `id`, `name`, `addr`, `short`, `primary`, `score`, `bal`,
`trades`, `win`, `pf`, `net`, `dd`, `spot`, `perp`, `d1`, `equity`, `pos`.
`spot` и `perp` — место в топе строкой вида `#12`, либо `—`.

## Ограничения восстановления

- Собрать и проверить проект в моей среде нельзя: нет сети, значит нет
  `npm install`. Сборка и проверка — на ВМ.
- `vite.config.ts` из шаблона Grok собирает под Vercel через `nitro`.
  Развёрнуто же статикой в nginx. Конфигурацию пишем заново под статику.
- Правки, сделанные прямо в бандле (переводы, форматирование чисел,
  шрифт, логотипы, экран «Мои кошельки»), уже учтены — восстановление
  идёт из текущей версии файла, а не из исходной.
